data : read all the data
features : read features
features.mean.std : keep standard deviation & mean 

data.mean.std : Extracts only the measurements on the mean and standard deviation for each measurement.
Create increment of 2 due to the labels and subjects in the file

labels : read activities
data.mean.std$label : replace activities labels with label names

good.colnames : Appropriately labels the data set with descriptive variable names
colnames(data.mean.std) : make a list of current names and tidy the list

aggr.data : find mean for each combination
write.table : write table
